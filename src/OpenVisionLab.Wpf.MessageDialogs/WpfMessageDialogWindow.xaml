<Window x:Class="OpenVisionLab.Wpf.MessageDialogs.WpfMessageDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message"
        Width="520"
        SizeToContent="Height"
        MaxHeight="420"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        KeyDown="OnWindowKeyDown">
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="{DynamicResource Surface.Panel}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource Text.Primary}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Accent.Hover}" />
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Surface.Hover}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Accent.Primary}" />
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Surface.Selected}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryDialogButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource Accent.Soft}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Accent.Primary}" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.Effect>
            <DropShadowEffect Color="#000000"
                              BlurRadius="24"
                              ShadowDepth="6"
                              Opacity="0.45" />
        </Grid.Effect>
        <Border Background="{DynamicResource Surface.Panel}"
                BorderBrush="{DynamicResource Border.Default}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="22">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border x:Name="KindBadge"
                        Width="44"
                        Height="44"
                        Margin="0,0,16,0"
                        VerticalAlignment="Top"
                        Background="{DynamicResource Accent.Soft}"
                        BorderBrush="{DynamicResource Accent.Primary}"
                        BorderThickness="1"
                        CornerRadius="22">
                    <TextBlock x:Name="KindGlyph"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Accent.Hover}"
                               FontSize="20"
                               FontWeight="Bold"
                               Text="?" />
                </Border>

                <StackPanel Grid.Column="1">
                    <Border x:Name="AccentBar"
                            Width="52"
                            Height="3"
                            Margin="0,0,0,12"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource Accent.Primary}"
                            CornerRadius="2" />
                    <TextBlock x:Name="TitleText"
                               Foreground="{DynamicResource Text.Primary}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap" />
                    <TextBlock x:Name="MessageText"
                               Margin="0,10,0,0"
                               MaxHeight="240"
                               Foreground="{DynamicResource Text.Secondary}"
                               FontSize="13"
                               LineHeight="20"
                               TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Margin="0,24,0,0"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                    <Button x:Name="PrimaryButton"
                            Style="{StaticResource PrimaryDialogButtonStyle}"
                            Click="OnPrimaryClicked" />
                    <Button x:Name="SecondaryButton"
                            Style="{StaticResource DialogButtonStyle}"
                            Click="OnSecondaryClicked" />
                    <Button x:Name="TertiaryButton"
                            Style="{StaticResource DialogButtonStyle}"
                            Click="OnTertiaryClicked" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
